import React from 'react';

import './Loading.scss';

const Loading = () => {
  return (
    <div className="modal">
      <div className="loadingAnimation" />
    </div>
  );
};

export default Loading;
