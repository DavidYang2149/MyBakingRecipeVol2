export const theBaseImageURL = process.env.NODE_ENV === 'production'
  ? (
    'https://davidyang2149.github.io/MyBakingRecipeVol2/static/css/'
  ) : (
    'http://localhost:9092/'
  );

// TODO: Remove this
export const XXX = '';
